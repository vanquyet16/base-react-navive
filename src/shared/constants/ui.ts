/**
 * UI CONSTANTS
 * ============
 * Layout, spacing, sizing constants for UI components
 */
import { moderateVerticalScale } from 'react-native-size-matters';

/**
 * Screen padding and margins
 */
export const SCREEN_PADDING = 16;

/**
 * Default border radius
 */
export const BORDER_RADIUS = 8;

/**
 * Chiều cao Bottom Tab Bar (responsive).
 * Dùng trong MainLayout để tính paddingBottom cho content.
 */
export const BOTTOM_TAB_HEIGHT = moderateVerticalScale(64);
